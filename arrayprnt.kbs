# ARRAY PRINT IN BASIC
dim a(10)	# Array of 10 Elements

# Fill the array with random numbers
print "Generating Random Numbers.."

for i = 0 to 9
	a[i] = rand * 10
	print a[i]
next i

# Find the Average
let s = 0
let l = 0

print "Finding average..."

for j = 0 to 9
	if a[j] > l then
		l = a[j]
	end if

	s = s + a[j]
next j

print "Average is : ", s/a[?]
print "Largest is : ", l

